# AucFan リサーチツール 取扱説明書

> 対象読者：アプリを使うビジネスユーザー（非エンジニア向け）

---

## 目次

1. [導入準備](#1-導入準備)
2. [ファイル構成](#2-ファイル構成)
3. [アプリの概要](#3-アプリの概要)
4. [起動方法](#4-起動方法)
5. [STEP 1 — キーワードリサーチ](#5-step-1--キーワードリサーチ)
6. [STEP 2 — セラーリサーチ](#6-step-2--セラーリサーチ)
7. [STEP 3 — マスターリスト横断リサーチ](#7-step-3--マスターリスト横断リサーチ)
8. [マスターリストタブ](#8-マスターリストタブ)
9. [結果グリッドの見方](#9-結果グリッドの見方)
10. [除外ルールについて](#10-除外ルールについて)
11. [過去セッションの表示方法](#11-過去セッションの表示方法)
12. [よくある質問](#12-よくある質問)
13. [Google Drive連携（複数Mac共有）](#13-google-drive連携複数mac共有)
14. [スタンドアロン運用（1台完結）](#14-スタンドアロン運用1台完結)
15. [Macの役割を入れ替える方法](#15-macの役割を入れ替える方法)（`switch_role.sh` でワンコマンド切り替え）
16. [Excelリサーチシートの出力](#16-excelリサーチシートの出力)
17. [Amazon調査タブ](#17-amazon調査タブ)
18. [今後追加予定の機能](#18-今後追加予定の機能)

---

## 1. 導入準備

> はじめてこのツールを使う方向けのセットアップ手順です。セットアップ済みの方は [2. アプリの概要](#2-アプリの概要) へ進んでください。

### 1-1. Homebrew のインストール

**Homebrew** は macOS 用のパッケージ管理ツールです。Python や Git などのソフトウェアをターミナルから簡単にインストールできます。

**インストール**（公式サイトのコマンドを1行コピーして実行）：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

> ターミナルを開くには：Finder → アプリケーション → ユーティリティ → ターミナル

**インストール確認**：

```bash
brew --version
```

`Homebrew 4.x.x` のように表示されれば成功です。

---

### 1-2. 必要なアプリ・ライブラリのインストール

#### Python 3.11以上

```bash
brew install python
```

インストール確認：`python3 --version`（`Python 3.11` 以上が表示されればOK）

#### Google Chrome

https://www.google.com/chrome/ からダウンロードしてインストールしてください。

> このツールは Chrome をリモート操作してスクレイピングを行うため、Chrome が必須です。

#### Pythonライブラリ（requirements.txt）

ターミナルで `aucfan_tool` フォルダに移動してから実行：

```bash
cd ~/Downloads/aucfan_tool
pip install -r requirements.txt --break-system-packages
```

`requirements.txt` に含まれる主なライブラリ：

| ライブラリ | 役割 |
|---|---|
| `flask` | Webサーバー（管理画面の表示） |
| `selenium` | ChromeをPythonから自動操作 |
| `imagehash` | 画像のpHash計算（同一商品グループ化） |
| `Pillow` | 画像処理 |
| `google-generativeai` | Gemini AIによる除外判定 |
| `pandas` | CSVの読み書き・データ操作 |
| `python-dotenv` | `.env` ファイルの読み込み |
| `requests` | 画像のダウンロード |
| `beautifulsoup4` | HTMLの解析 |

---

### 1-3. Gemini APIキーの取得と設定

#### APIキーの取得手順

1. ブラウザで https://aistudio.google.com/ を開く
2. Googleアカウントでサインイン
3. 左メニューの「**Get API key**」をクリック
4. 「**Create API key**」ボタンを押す
5. 表示されたキー（`AIzaSy...` で始まる文字列）をコピーする

#### `.env` ファイルへの保存

`aucfan_tool` フォルダの中に `.env` というファイルがあります（隠しファイルのため Finder では非表示）。テキストエディタで開いて以下を記載：

```
GEMINI_API_KEY=AIzaSy...（取得したキーを貼り付け）
```

ターミナルから作成・編集する場合：

```bash
echo 'GEMINI_API_KEY=AIzaSy...' >> ~/Downloads/aucfan_tool/.env
```

> **セキュリティについて**: `.env` ファイルは `.gitignore` に登録されているため、`git push` しても GitHub にはアップロードされません。APIキーが外部に漏れる心配はありません。

---

### 1-4. GitHub の使い方

コードの同期・バックアップ用途のセクションです。チームで共有したり、最新バージョンに更新するために使います。

#### 初回セットアップ

**GitHubアカウント作成**：https://github.com でアカウントを作成してください。

**Gitのインストール**：

```bash
brew install git
```

**初回設定**（自分の名前とメールアドレスを登録）：

```bash
git config --global user.name "あなたの名前"
git config --global user.email "your@email.com"
```

**リポジトリのクローン**（初回のみ・コードをダウンロード）：

```bash
git clone https://github.com/halhah-stack/aucfan_tool.git ~/Downloads/aucfan_tool
```

#### 日常の使い方（3コマンド）

**最新コードを取得**（チームメンバーの変更を自分のPCに反映）：

```bash
cd ~/Downloads/aucfan_tool
git pull origin main
```

**変更をアップロード**（自分の変更をGitHubに保存）：

```bash
cd ~/Downloads/aucfan_tool
git add -A
git commit -m "変更内容のメモ（例: 除外キーワードを追加）"
git push origin main
```

**現在の状態確認**：

```bash
git status
```

#### 注意事項

以下のフォルダ・ファイルは `.gitignore` で管理対象外になっており、GitHub にはアップロードされません（個人データ・APIキーの保護のため）：

- `.env` — Gemini APIキー等の秘密情報
- `リサーチ結果/` — スクレイピング結果データ
- `data/` — マスターセラーリスト

**`index.lock` エラーが出た場合**（Gitが中途半端な状態で終了したとき）：

```bash
rm ~/Downloads/aucfan_tool/.git/index.lock
```

---

### 1-5. アプリの起動確認

セットアップが完了したら、以下のコマンドでアプリを起動して動作を確認してください：

```bash
bash ~/Downloads/aucfan_tool/start.sh
```

ブラウザで `http://localhost:5001` が開けば導入完了です。

---

### 1-6. 別端末（2台目Mac）へのセットアップ

2台目のMacでこのツールを動かす場合、以下の手順を順番に実行してください。1台目ですでに完了している作業（Homebrew・Gitのインストール、APIキーの取得）は省略できます。

#### ① コードの取得

```bash
git clone https://github.com/halhah-stack/aucfan_tool.git ~/Downloads/aucfan_tool
```

#### ② Python仮想環境のセットアップ

```bash
cd ~/Downloads/aucfan_tool
pip install -r requirements.txt --break-system-packages
```

#### ③ .env の作成

`~/Downloads/aucfan_tool/.env` ファイルを新規作成し、最低限以下を記載してください：

```
GEMINI_API_KEY=AIzaSy...（取得済みのAPIキー）
```

Google Driveでリサーチデータを複数Mac間で共有する場合は、`OUTPUT_BASE_DIR` と `SELLERS_MASTER_PATH` も追加で設定します（詳細は [§13 Google Drive連携](#13-google-drive連携複数mac共有) を参照）。

#### ④ 起動

```bash
bash ~/Downloads/aucfan_tool/start.sh
```

ブラウザで `http://localhost:5001` が開けばセットアップ完了です。

---

## 2. ファイル構成

> **コードを触らない方もここだけ把握しておくと便利です。** どのファイルが何の役割を持っているか知っておくと、設定変更や困ったときのトラブルシュートがスムーズになります。

### プログラムファイル（Pythonコード）

| ファイル名 | 役割 |
|---|---|
| `app.py` | アプリのサーバー本体。起動・ボタン操作の受け取り・各処理の指示出しを担う中心ファイル |
| `scraper.py` | AucFanをスクレイピングするメインプログラム。Chromeを自動操作して商品一覧・詳細を取得する |
| `seller_analyzer.py` | STEP 2専用。セラーIDごとに商品ページを巡回して取得する処理 |
| `sellers_master.py` | マスターセラーリスト（`data/sellers_master.json`）の読み書きを管理する |
| `data_manager.py` | セッションフォルダの作成・商品データの保存・CSV書き出しを管理する |
| `gemini_client.py` | Gemini AIに「この商品は除外すべきか？」を問い合わせる処理 |
| `image_processor.py` | 商品画像をダウンロードしてpHash（画像の類似度判定）を計算し、同一商品をグループ化する |
| `config.py` | 価格帯・待機時間・除外キーワードリストなど**全設定値が集まっているファイル**。設定を変えたいときはここか`.env`を見る |

### 設定・環境ファイル

| ファイル名 | 役割 |
|---|---|
| `.env` | APIキー・価格帯・件数設定など個人ごとの設定値。**GitHubにはアップロードされない**（`.gitignore`で除外済み） |
| `start.sh` | アプリの起動スクリプト。ChromeをデバッグモードでいちどRelaunchしてFlaskを立ち上げる |
| `requirements.txt` | このアプリに必要なPythonライブラリ一覧。`pip install -r requirements.txt` で一括インストールできる |

### 画面ファイル（フロントエンド）

| ファイル名 | 役割 |
|---|---|
| `templates/index.html` | ブラウザに表示されるアプリの画面HTML |
| `static/app.js` | 画面のボタン動作・グリッド表示・タブ切り替えなどの処理 |
| `static/style.css` | 画面のデザイン（色・レイアウト・フォント等） |

### データフォルダ

| フォルダ / ファイル | 役割 |
|---|---|
| `リサーチ結果/` （ローカル or Google Drive） | スクレイピング結果の保存先。デフォルトは Google Drive の `AucFanToolData/リサーチ結果/`。`S1_YYYYMMDD_XX_キーワード/`・`S2_/`・`S3_/` という名前のフォルダが増えていく |
| `Google Drive/AucFanToolData/sellers_master.json` | マスターセラーリストのデータ本体。STEP 1完了のたびに自動更新される（Google Drive 未接続時は `data/sellers_master.json` にフォールバック） |
| `docs/` | 本書（`USER_GUIDE.md`）とエンジニア向けの`CODE_GUIDE.md`が入っているフォルダ |

> **よく変更するファイル TOP 3**: ①`.env`（価格・件数の調整）、②`config.py`（除外キーワードの追加・CSSセレクターの修正）、③`prompts.yaml`（Geminiへの指示文の調整）

---

## 3. アプリの概要

**AucFan リサーチツール**は、ヤフオク・メルカリ等の落札データを集計しているサイト「AucFan」から商品情報を自動収集し、**仕入れ候補となるノーブランド商品**を効率よく発見するためのツールです。

### 主な機能

| 機能 | 内容 |
|------|------|
| STEP 1 キーワードリサーチ | キーワードで検索 → 同一商品をグループ化 → 仕入れ候補を自動抽出 |
| STEP 2 セラーリサーチ | 気になるセラーの全商品を横断スクレイピング |
| STEP 3 マスターリスト横断 | 蓄積したセラーIDをまとめてリサーチ |
| AI除外判定 | 食品・ブランド品・危険物等を Gemini AI が自動除外 |
| 結果エクスポート | CSV・HTML形式でデータを書き出し |

### 仕組みのイメージ

```
AucFan（ブラウザで開いた状態）
        ↓  Selenium で自動操作
  商品データ収集（1ページ50件×数百ページ）
        ↓
  同一商品グループ化（pHash画像認識）
        ↓
  AI除外判定（Gemini Vision/テキスト）
        ↓
  結果グリッドに表示 → セラーIDがマスターリストに自動追加
```

---

## 4. 起動方法

### 手順

1. **Macのターミナルを開く**（Finder → アプリケーション → ユーティリティ → ターミナル）

2. **ツールフォルダに移動してスクリプトを実行する**

   ```bash
   bash ~/Downloads/aucfan_tool/start.sh
   ```

3. **自動で以下が実行される**
   - Chromeが一度終了し、デバッグモードで再起動
   - Flaskサーバーが起動（ポート5001）
   - ブラウザで `http://localhost:5001` が自動的に開く

4. **Chromeの新しいタブで AucFan を開く**
   - `https://aucfan.com` にアクセスしてログイン
   - 検索したいキーワードで検索し、**1ページ目を表示した状態にする**

5. **管理画面のタブに戻ってスクレイピングを操作する**

> **iPhone/iPad からも操作可能** — 同じWi-Fi上であれば `http://[MacのIPアドレス]:5001` でアクセスできます。

### 停止方法

ターミナルで `Ctrl + C` を押すと Flask サーバーが停止します（Chromeはそのまま残ります）。

---

## 5. STEP 1 — キーワードリサーチ

AucFanで特定のキーワードを検索し、同一商品が複数出品されているグループを探します。

### 手順

#### Macの場合（推奨）

1. 「STEP 1 キーワードリサーチ」タブを選択
2. 「キーワード」欄に検索語を入力（例：`LEDライト`）
3. Chromeで AucFan を開き、同じキーワードで検索して1ページ目を表示しておく
4. **「▶ スクレイピング開始」**をクリック
5. 進捗バーが表示され、商品が順次グリッドに追加される
6. 「■ 停止」で任意のタイミングで中断可能

#### iPhone/iPad からの場合

- **方法①（推奨）**：SafariでAucFanを検索 → URLをコピー → 「AucFan 検索URLを貼り付ける」欄に貼り付け → スクレイピング開始
- **方法②**：「🔍 Chromeのタブを確認」ボタンを押してMacで開いているAucFanタブを選択

### 進捗・完了表示

スクレイピング中は以下の表示でリアルタイムに確認できます。

- 画面上に「**X件 / Y件処理済み**」というカウンターが表示されます
- 完了すると画面上部に緑色の「**✅ スクレイピング完了（N件処理）**」バナーが表示されます（✕ボタンで閉じられます）
- ターミナルにも「`=== STEP 1 スクレイピング完了 === 全N件処理`」という目立つログが出力されます

### スクレイピング完了後

- 結果グリッドに商品グループが表示される
- **セラーIDがマスターリストに自動追加**（同一商品グループが2件以上のセラーのみ）
- 結果はセッションフォルダ（`リサーチ結果/S1_YYYYMMDD_01_キーワード/`）に自動保存される

### 結果の確認

| 表示 | 意味 |
|------|------|
| 🔵 仕入れ候補 | 同一商品が5件以上のグループ（設定変更可） |
| 🟡 次期候補 | 同一商品が4件以上のグループ |
| ⏳ 確認待ち | AI判定前の商品 |
| ⚠️ 要確認 | AIが安全リスクフラグを立てた商品 |
| ✅ OK | ユーザーが承認した商品 |
| ❌ NG | 除外した商品 |

---

## 6. STEP 2 — セラーリサーチ

特定のセラーの全商品をスクレイピングし、セラーをまたいで同一商品を比較します。

> **新品のみ取得**：STEP 2・STEP 3では AucFan 一覧ページの「商品状態」欄が「新品・未使用・未開封・未着用」の商品のみを取得します（Amazon中国輸入リサーチ用途のため、中古品は自動除外）。中古品を含めたい場合は `.env` に `SELLER_NEW_ONLY=false` を追加してください。

### 手順

1. 「STEP 2 セラーリサーチ」タブを選択

2. **セラーIDの取得方法を選ぶ（3通りあります）**

   **⚡ 最速（推奨）**：「🔗 現在のSTEP 1結果からセラーIDを取得」ボタンを押す
   - STEP 1の結果がメモリにある場合、CSVなしで直接取得できる

   **Google DriveのSTEP 1セッションから**：🔄ボタンでセッション一覧を更新 → 使いたいセッションを選択

   **CSVファイルから**：「💾 最新の保存済みCSVを使用」または「📂 CSVファイルを選択」

3. セラーIDの一覧が表示されたら「**▶ スクレイピング開始**」をクリック

4. 各セラーの商品が順次スクレイピングされ、全完了後に結果グリッドに表示される

### 進捗・完了表示

- 画面上に「**X件 / Y件処理済み**」というカウンターが表示されます
- 完了すると画面上部に緑色の「**✅ スクレイピング完了（N件処理）**」バナーが表示されます（✕ボタンで閉じられます）
- ターミナルにも「`=== STEP 2 スクレイピング完了 === 全N件処理`」という目立つログが出力されます
- 中古品を除外したページは「`[商品状態フィルタ] N件除外（中古等）→ 次ページへ`」とログに表示され、自動で次ページに進みます
- **ログイン切れを検知した場合**はヘッダーが「⚠️ ログイン待ち」に変わり、バナーに「🔄 今すぐ確認して再開」ボタンが表示されます。ログイン後は自動（30秒）またはボタン押下で即再開します

### 中古セラーの自動スキップ

1セラーのスクレイピング中に中古商品がデフォルト **3件** を超えると、そのセラーを「中古主体セラー」と判断して打ち切り、取得済みデータを自動削除して次のセラーへ進みます。

- セラーリストに **「🚫 中古N件超でスキップ」**（オレンジ・件数入り）と表示されます
- ターミナルにも理由と件数が表示されます（例：`🚫 中古セラースキップ: seller_abc123 / 中古件数: 7件（閾値: 5件超でスキップ）`）
- 閾値は `.env` の `SELLER_USED_SKIP_THRESHOLD` で変更できます（デフォルト：`5`）
- `0` に設定するとスキップ無効（アイテム単位フィルターのみ）

> **なぜ5件許容するか**：中国輸入が主力のセラーでも、過去に1〜2件だけ中古を出品したことがある場合があります。1件でも即スキップすると正規セラーを誤除外するおそれがあるため、デフォルト5件（6件目でスキップ）の許容幅を設けています。

### 結果の確認

セラー分析モードでは、同一商品をセラーをまたいでグループ化して表示します。セラー分析専用フィルターで件数を絞り込めます。

> **注意**：STEP 1でキーワード＋新品検索をしてもセラーが新品・中古両方を出品している場合があります。STEP 2ではそのセラーの全出品ページを巡回しますが、商品状態フィルタ・中古セラースキップにより中古品は自動除外されます。

---

## 7. STEP 3 — マスターリスト横断リサーチ

マスターリストに蓄積された「未スクレイピング」のセラーIDをまとめてリサーチします。STEP 1を繰り返すたびにセラーが自動追加されていくため、定期的にSTEP 3を実行することで効率よく新規セラーを発掘できます。

### 手順

1. 「STEP 3 マスターセラーリサーチ」タブを選択

2. **設定を確認する**

   | 設定 | 説明 |
   |------|------|
   | 並び順 | 「新しい順」= 最近追加されたセラーから処理／「古い順」= 古いセラーから処理 |
   | バッチサイズ | 1回の実行で処理するセラー数（50/100/200/上限なし） |

3. 「対象: ○○件」のプレビューを確認して「**▶ スクレイピング開始**」をクリック

4. スクレイピングが完了すると結果グリッドに表示される

### 進捗・完了表示

- 画面上に「**X件 / Y件処理済み**」というカウンターが表示されます
- 完了すると画面上部に緑色の「**✅ スクレイピング完了（N件処理）**」バナーが表示されます（✕ボタンで閉じられます）
- ターミナルにも「`=== STEP 3 スクレイピング完了 === 全N件処理`」という目立つログが出力されます
- **ログイン切れを検知した場合**はSTEP 2と同様にバナーと「🔄 今すぐ確認して再開」ボタンが表示されます

> 未スクレイピングのセラーが0件の場合は実行できません。STEP 1でキーワードリサーチを行うとセラーが追加されます。

---

## 8. マスターリストタブ

STEP 1実行のたびに自動追加されるセラーIDを管理するタブです。

### 表示項目

| 列 | 説明 |
|----|------|
| セラーID | AucFanのセラー識別子 |
| 初回検出日 | このセラーが最初にマスターリストに追加された日付 |
| 最終スクレイピング | 最後にSTEP 3でスクレイピングした日付（未実施は「未」） |
| 候補件数 | 最後のSTEP 3で見つかった仕入れ候補の件数 |
| キーワード | 初回検出時のSTEP 1キーワード |

### 操作ボタン

| ボタン | 機能 |
|--------|------|
| 🔄 更新 | リストを最新状態に更新 |
| 💾 CSV保存 | マスターリスト全件をCSVでダウンロード |
| 📄 HTML書き出し | 検索機能付きスタンドアロンHTMLを書き出し（iPhone等で見る用） |
| 📂 CSV読み込み | 外部のCSVファイルからセラーIDをインポート（`seller_id`列が必要） |
| 📥 別Macのリストをマージ | 別のMacで蓄積した `sellers_master.json` をインポートしてマージ（重複排除） |
| 🗑 全削除 | マスターリストを全件削除（確認あり） |
| 各行の 🗑 | その行のセラーIDを個別削除（ホバーで表示） |

### マスターリストのマージ

「📥 別Macのリストをマージ」ボタンを使うと、別のMacで蓄積した `sellers_master.json` をインポートして現在のリストに統合できます。

- `seller_id` をキーとして重複排除します。既存のエントリは上書きされず、初回追加日等のデータがそのまま保持されます
- マージ完了後に「**N件追加、M件スキップ（重複）、合計K件**」という結果メッセージが表示されます
- **用途の例**：Google Drive設定前に各Macで別々に蓄積したリストを統合したい場合など

---

## 9. 結果グリッドの見方

### グループカードの構成

```
┌─────────────────────────────────────┐
│ 🔵 仕入れ候補       同一商品 [7件]  │  ← ステータスバー
├─────────────────────────────────────┤
│ [画像] [画像] [画像] [画像] [画像]  │  ← サムネイル（最大5枚）
├─────────────────────────────────────┤
│ 商品タイトル（最大2行表示）          │
│ 合計: ¥2,480  落札価格: ¥2,200      │
│       送料: ¥280                    │
│ 出品者: seller01  seller02          │
├─────────────────────────────────────┤
│ [OK] [NG] [詳細] [AliPrice] [Amazon]│  ← 操作ボタン
└─────────────────────────────────────┘
```

### フィルター機能

グリッド上部のフィルターバーで絞り込みができます。

- **ステータス**：仕入れ候補・確認待ち・NG 等で絞り込み
- **価格範囲**：合計価格の最小〜最大を指定
- **最小グループ数**：同一商品の最小件数で絞り込み
- **キーワード**：タイトルでテキスト検索

### NG一覧

画面右上に「❌ NG一覧を表示（○件）」ボタンが表示されます。NGにした商品を一括確認できます。NGと判定された商品には、判定の根拠（ソース）がバッジで表示されます。

| バッジ | 意味 |
|---|---|
| タイトルキーワード | 商品タイトルに含まれるキーワードにより除外 |
| メーカー名 | 商品タイトル先頭のメーカー・ブランド名により除外 |
| Gemini（テキスト） | GeminiのテキストAIによる判定で除外 |
| Gemini（Vision） | GeminiのVision AIが画像を確認して除外 |

### ソート（セラー分析モード）

STEP 2/3の結果は、件数順・価格順でソートできます（グリッド上部のソートバー）。主なソートオプションの使い分けは以下の通りです。

| ソート | 説明 |
|---|---|
| 件数×価格（高） | 同一商品グループの件数が多く、かつ価格が高いグループを上位表示。複数のセラーが同じ商品を高値で出品しているものを優先的に確認できます |
| 件数×価格（低） | 件数が多く価格が安いグループを優先表示。仕入れ単価を抑えたい場合に有効です |

### ページ送り

グリッドの右上・右下にページネーションが表示されます。1ページあたり30グループが表示されます。

### エクスポート

| ボタン | 機能 |
|--------|------|
| 📊 CSVエクスポート | 現在の結果をCSVでダウンロード |
| 💻 HTMLで保存（Mac用） | 検索・フィルター付きHTMLを書き出し（画像はサーバー経由）。ファイル名に `_Mac用` が付きます |
| 📱 HTML保存（iPhone用） | 全件を画像圧縮済みBase64埋め込みHTMLで書き出し。Google Driveの `AucFanToolData` フォルダに保存されます。ファイル名に `_iPhone表示用` が付きます |
| 📱 HTML保存（候補のみ・軽量） | 候補・OK・要確認ステータスの商品のみに絞って書き出し（ファイルサイズ削減）。件数が多くてiPhone Safariで開けないときはこちら。ファイル名に `_iPhone表示用_候補のみ` が付きます |
| 📂 CSV読み込み | 保存済みのCSVをグリッドに再表示 |

---

## 10. 除外ルールについて

スクレイピングした商品は複数の段階で除外判定が行われます。

### 除外ルールのメンテナンス

**除外ルールはすべて `rules.yaml` 1ファイルで管理します。**

```
aucfan_tool/
└── rules.yaml   ← ここだけ編集すればOK
```

**メンテ手順：**
1. `rules.yaml` をテキストエディタで開く
2. 該当セクションにキーワードを追記する
3. `bash start.sh` でアプリを再起動する

**セクション説明：**

| セクション | 用途 |
|---|---|
| `title_keywords` | タイトル文字列に含まれれば即除外（高速・確実） |
| `maker_keywords` | タイトル先頭のメーカー・ブランド名で除外 |
| `trading_card_keywords` | トレカ関連（Gemini判定に委ねる） |
| `automotive_keywords` | 自動車・バイク関連（メーカー除外をスキップ） |
| `custom_rules` | Gemini学習ルール（NG理由分析で提案されたもの） |

**NGボタンから除外キーワードを追加する手順：**

1. スクレイピング結果でNGな商品を見つける
2. ❌NGボタン → 理由を入力（例：「フライパンなのでNG」）
3. Gemini分析パネルが表示される（カテゴリ・理由・キーワード候補）
4. `rules.yaml` の `title_keywords` に候補キーワードを追記
5. `custom_rules` にも理由付きで記録しておくと後で参照できる
6. `bash start.sh` で再起動 → 次回から自動除外される

```yaml
# rules.yaml の記入例
title_keywords:
  - フライパン    ← ここに追記

custom_rules:
  - keyword: フライパン
    category: 衛生リスク商品
    reason: 食品と直接接触する調理器具は中古品として衛生リスクがある
    added: 2026-05-21
```

---

### 除外される商品の種類

**タイトルキーワード除外**（スクレイピング時点で弾く）
- チケット・金券・優待券
- 食品・食材・飲料全般（水産物・農産物・肉類・お菓子・酒類 等）
- 危険物・高圧ガス（エアコンガス・スプレー缶 等）
- 特定球団グッズ・サイン色紙 等
- **身体への影響リスク商品**：シャンプー・リンス・トリートメント・育毛剤・ヘアカラー・マスク・フェイスパック・化粧品・スキンケア・ボディソープ・除毛クリーム・制汗剤・歯磨き粉・サプリメント・医薬品・消毒液 等
- **生体・動物**：ニワトリ品種（「有髭」等のbearded品種含む）・観賞魚・メダカ・カブトムシ・インコ・ハムスター 等（Amazon輸入対象外のため除外）
- **植物の苗・種子**：多肉植物・サボテン・球根 等

**タイトル先頭メーカー名除外**
- タイトルの先頭1〜2番目にHITACHI・Panasonic・Sony・Apple等の有名メーカー名が来る商品
- ただし、**自動車・バイク関連キーワードを含む商品はこの除外対象外**（下記「自動車・バイク関連のバイパスルール」を参照）

**自動車・バイク関連のバイパスルール**
- 自動車・バイク・カー用品に関連するキーワード（車種名・「カー用品」「バイク用品」等）が商品タイトルに含まれる場合、メーカー名が先頭にあっても除外されません
- **理由**: カー用品・バイク用品はメーカー互換品・汎用品が多く、ブランド名が商品説明に含まれていても仕入れ対象になりうるためです
- **例**:
  - 「トヨタ キーケース」→ 除外されない（カー用品系キーワードにより除外をバイパス）
  - 「トヨタ 純正部品」→ 除外される（「純正部品」は自動車バイパスキーワードに含まれないため通常の除外対象）

**Gemini AI テキスト判定**（`詳細取得あり`モードのみ）
- 絶対除外カテゴリ（食品・衣類・AC100V家電・危険物・トレカ本体）
- 要確認フラグ（車の保安部品等）
- 有名ブランド品

**Gemini AI Vision判定**（同一商品グループが4件以上のグループ）
- 画像を直接見て総合判定（1回のAPIコールで除外・要確認・ブランド品を判定）

> Gemini APIキーが設定されていない場合はAI判定がスキップされ、pHash（画像ハッシュ）によるグループ化のみで動作します。

---

## 11. 過去セッションの表示方法

各STEPパネルの下部に「📂 過去のセッション」セクションがあります。

### 操作手順

1. 目的のSTEPタブを開く（例：STEP 1）
2. 「📂 過去のSTEP 1セッション」セクションの「🔄 更新」ボタンを押す
3. セッション一覧が表示される
4. 表示したいセッションの「**📂 表示**」ボタンを押す
5. そのセッションの結果グリッドが表示され、画面上部に「📌 表示中: セッション名（件数）」が表示される

### セッションフォルダ名の見方

| 形式 | 意味 |
|------|------|
| `S1_20260506_01_バフ` | STEP 1、2026/05/06実行、当日1回目、キーワード「バフ」 |
| `S2_20260506_01` | STEP 2、2026/05/06実行、当日1回目 |
| `S3_20260506_01` | STEP 3、2026/05/06実行、当日1回目 |

---

## 12. よくある質問

### AucFanのログインが切れてしまった（複数Macで同時ログインした場合など）

AucFanは2台のMacで同じアカウントにログインすると、先にログインしていた側がゲスト状態になることがあります。スクレイピング中にログアウトが検知されると、自動的に以下の動作をします。

**自動検知と待機：**
1. ページ上に「ようこそ、 ゲストさん」の文言を検出するとログアウトと判断
2. ヘッダーが「**⚠️ ログイン待ち**」に切り替わる
3. 画面上部に**琥珀色のバナー**「AucFanのログインが切れました」が表示される
4. ターミナルにも警告と再開予定URLが表示される
5. **30秒ごとに自動でログイン状態を確認**し、ログインを検知したら自動で再開する

**すぐに再開したい場合：**
- バナー右端の「**🔄 今すぐ確認して再開**」ボタンを押す（タブ移動なし・即時確認）

**注意点：**
- タブはSTEP 2のままで移動しません（LocalStorageで位置を保持）
- スクレイピング再開後、pHash画像グループ化・Vision判定も自動で続きます
- STEP 1・STEP 2・STEP 3すべてで動作します

---

### スクレイピングが突然止まった

**原因として多いもの：**
- AucFanがメンテナンス中またはサーバーエラー
- Chromeのセッションが切れた
- ネットワークが不安定

**対処法：**
1. 「■ 停止」ボタンを押して完全に停止させる
2. AucFanのページをリロードする
3. 再度「▶ スクレイピング開始」を押す（途中から再開される場合があります）

---

### 商品が少ししか表示されない

**考えられる原因：**
- 検索結果自体が少ない（AucFanで検索してページ数を確認）
- 価格フィルターで多くの商品が除外されている（デフォルト: 1,000〜3,000円）
- 同一商品グループが少ない（グループ化の閾値が高い）

**対処法：**
- `.env` ファイルの `MIN_PRICE` / `MAX_PRICE` を変更して価格範囲を広げる
- フィルターバーの「最小グループ数」を「1」に下げて全商品を表示する

---

### マスターリストへの追加条件は？

STEP 1完了時に、pHashグループ化後の**グループサイズが2件以上**のグループに含まれるセラーIDのみが追加されます。1件のみの商品（ユニーク品）のセラーは追加されません。

この閾値は `.env` の `MASTER_SELLER_MIN_GROUP_SIZE` で変更できます（デフォルト: 2）。

---

### AI判定が動かない

Gemini APIキーが設定されていない、または無効の可能性があります。

**確認方法：**
1. `~/Downloads/aucfan_tool/.env` ファイルをテキストエディタで開く
2. `GEMINI_API_KEY=` の後ろにAPIキーが入っているか確認
3. キーがない場合は [Google AI Studio](https://aistudio.google.com/app/apikey) で取得して設定

AI判定なしでも pHash による画像グループ化は動作します。

---

### 画面上部にエラーのバナーが表示された

スクレイピング中にGemini APIでエラーが発生すると、画面上部にオレンジ・赤・黄色のバナーが表示されます。✕ボタンで閉じることができます。

以下はエラーの種類と対処法の一覧です。

| エラー種別 | 表示色 | 意味 | 対処法 |
|---|---|---|---|
| レート制限超過（429） | オレンジ〜赤 | 無料枠の1日または1分あたりのリクエスト上限に達した | しばらく待つか、Google AI Studioで従量課金に切り替える |
| 一時混雑（503） | 黄色 | Geminiサーバーが混雑している | 数分待って再スクレイピングする |
| 内部エラー（500） | 黄色 | Gemini側で一時的なエラーが発生した | 判定はスキップして続行している。再スクレイピングで回復することが多い |
| APIキーエラー（403） | 赤 | APIキーの権限不足または無効 | `.env` の `GEMINI_API_KEY` を確認する。必要に応じて [Google AI Studio](https://aistudio.google.com/app/apikey) で新しいキーを発行する |
| 入力エラー（400） | 黄色 | 画像が壊れているなど入力データの問題 | 該当商品はスキップして続行している。特別な対応は不要 |

> **スクレイピングは止まらない**: 上記エラーのうち503・500・400は、AI判定をスキップしてスクレイピング自体は継続されます。429（レート制限）と403（権限エラー）のみ、AI判定が一時停止または不能になる場合があります。

**無料枠について**

Gemini APIの無料枠は1日1,500リクエスト・1分あたり15リクエストが目安です。毎日複数回スクレイピングを行う場合は従量課金（月数百円程度）への切り替えを検討してください。429エラーが頻発するようになったら切り替えのサインです。従量課金への切り替えは [Google AI Studio](https://aistudio.google.com/app/apikey) の課金設定から行えます。

---

### 結果をiPhoneで確認したい

3つの方法があります。

**方法①：同じWi-Fiで管理画面を直接開く（最も確実・推奨）**
1. MacとiPhoneを同じWi-Fiに接続
2. MacのIPアドレスを確認（ターミナルで `ipconfig getifaddr en0`）
3. iPhoneのSafariで `http://[MacのIPアドレス]:5001` を開く

> Macアプリが起動していればリアルタイムでアプリと同じ画面が見られます。ファイル転送不要で最も軽快です。

**方法②：Google DriveのHTMLファイルをSafariで開く（オフライン確認用）**
1. フィルターバーの「📱 HTML保存（iPhone用）」または「📱 HTML保存（候補のみ・軽量）」をクリック
2. Mac上の Google Drive `AucFanToolData` フォルダに自動保存されるので、iPhoneの Google Drive アプリで同期を確認
3. iPhoneのファイルアプリまたはGoogle Driveアプリでファイルをタップ → **共有ボタン（□↑）→「Safariで開く」** を選択

> 件数が多くてSafariで開けない場合は「候補のみ・軽量」ボタンを使うとファイルサイズが大幅に小さくなります。画像は120×120px・JPEG圧縮済みのため、元データより数分の1のサイズになります。

**方法③：AirDropで直接送る**
1. 上記②と同様にHTMLを生成
2. MacのFinderで `AucFanToolData` フォルダ内のHTMLファイルを右クリック →「共有」→「AirDrop」でiPhoneに送信
3. iPhoneで受信後、ファイルアプリを開き 共有ボタン →「Safariで開く」

> いずれの方法でも、HTMLファイルはデータの読み込みには使えません（閲覧専用）。データを別のMacや別のアプリで再利用したい場合はCSVをお使いください。

---

### CSV読み込み時にHTMLファイルを誤って選択してしまった

CSV読み込みボタンでHTMLファイルを選択した場合、赤いトーストメッセージが表示されてアップロードは自動的にキャンセルされます。

| 選択したファイル | メッセージ |
|---|---|
| `〇〇_iPhone_iPad用.html` | 「このファイルはiPhone/iPad閲覧用のHTMLです」 |
| `〇〇_Mac用.html` | 「このファイルはMac閲覧用のHTMLです」 |

この検出はSTEP 2・STEP 3・マスターリストのCSV読み込みすべてに対応しています。データの再読み込みには必ず `.csv` ファイルをお使いください。

---

### セラー分析で「seller_url: なし」と表示される

STEP 1のスクレイピング時にセラーURLが取得できなかった場合に表示されます。この場合はフォールバックURL（`https://aucfan.com/search1/?aucnm=セラーID`）が自動的に使用されるため、スクレイピング自体は問題なく実行できます。

---

---

## 13. Google Drive連携（複数Mac共有）

2台のMacで役割を分担してデータを共有します。

| Mac | 役割 | `.env` の設定 | GDriveアプリ |
|-----|------|--------------|-------------|
| 十王Mac（scraper） | スクレイピング・結果をGDrive APIでアップロード | `SITE_ROLE=scraper` / `GDRIVE_UPLOAD_ENABLED=true` | ストリーミングモード |
| 守谷Mac（reader） | GDriveミラーリングで自動同期・閲覧のみ | `SITE_ROLE=reader` / `GDRIVE_UPLOAD_ENABLED=false` | ミラーリングモード |

> **役割を入れ替えたい場合は [§15 Macの役割を入れ替える方法](#15-macの役割を入れ替える方法) を参照してください。**

### 13-1. Google Drive for Desktop のインストール

1. https://www.google.com/drive/download/ から「Google Drive for Desktop」をダウンロード・インストールする
2. インストール後、`shinozakistore@gmail.com` でサインインする
3. **十王Mac**：「ストリーミング」モードに設定（クラウド上のファイルは必要なときだけダウンロード）
4. **守谷Mac**：「ミラーリング」モードに設定（全ファイルをローカルに同期・画像が即座に表示できる）

ミラーリング設定後、Google Driveアプリを開き → 環境設定 → 「マイドライブ」タブ → 「このデバイスにファイルをミラーリング」を選択します。同期が完了すると `~/マイドライブ（メールアドレス）/` というフォルダが作成されます。

### 13-2. credentials.json の取得と配置（十王Macのみ）

十王Macはスクレイピング結果をGoogle Drive APIで直接アップロードするため、**OAuth認証用ファイル**が必要です。

#### credentials.json とは

Google Cloud Consoleで発行する「このアプリがGoogle Driveにアクセスしてよい」という許可証です。Google Cloud Console からダウンロードしたときのファイル名は `client_secret_xxxxxxxxx.apps.googleusercontent.com.json` ですが、`aucfan_tool` フォルダに **`credentials.json` という名前でコピー**して使います。

#### 配置手順

```
1. AirDrop または Google Drive 経由で credentials.json を十王Macに転送
2. ~/Downloads/aucfan_tool/credentials.json として配置
```

> `credentials.json` は Git 管理外（`.gitignore` 登録済み）のため、`git push` してもGitHubには上がりません。

#### 初回OAuth認証

```bash
cd ~/Downloads/aucfan_tool
.venv/bin/python setup_gdrive_auth.py
```

ブラウザが開くので Googleアカウントにログインして「許可」をクリックすると `token.json` が自動生成されます。

```
✅ Google Drive に接続できました。アカウント: shinozakistore@gmail.com
```

`token.json` も Git 管理外です。**`credentials.json` と `token.json` は他人に見せないでください**（Googleアカウントへのアクセス権が含まれます）。

### 13-3. データ保存先

| 保存先（Google Drive上） | 内容 | 書き込み元 |
|---|---|---|
| `AucFanToolData/リサーチ結果/セッション名/` | items.json・results.csv・result.pdf | 十王Mac（API） |
| `AucFanToolData/リサーチ結果/セッション名/images/` | スクレイピング画像 | 十王Mac（API・完了後一括） |
| `AucFanToolData/` | 閲覧用HTML・仕入れ候補PDF | 十王Mac（API） |
| `AucFanToolData/sellers_master.json` | マスターセラーリスト（2拠点で共有） | 十王Mac |

> 画像アップロードはスクレイピング完了後にまとめて実行されます（スクレイピング中の速度低下を防ぐため）。

### 13-4. .env の設定（2台運用時）

**十王Mac（scraper）の `.env`**

```dotenv
SITE_ROLE=scraper
GDRIVE_UPLOAD_ENABLED=true
# ↑ この2行だけ設定すればOK。パスは config.py が自動検出する。
GEMINI_API_KEY=AIzaSy...（取得済みのキー）
```

**守谷Mac（reader）の `.env`**

```dotenv
SITE_ROLE=reader
GDRIVE_UPLOAD_ENABLED=false
# ↑ この2行だけ設定すればOK。GDriveミラーリングフォルダを自動検出する。
GEMINI_API_KEY=AIzaSy...（取得済みのキー）
```

### 13-5. 動作確認

十王MacでSTEP 1 を実行し、スクレイピング完了後に Google Drive の `AucFanToolData/リサーチ結果/` にセッションフォルダと画像が作成されれば設定完了です。守谷MacのGoogle Driveが同期されると、守谷MacのアプリでもSTEP 1 のセッションと画像が表示されます。

> **注意**: 2台のMacで同時にスクレイピングを実行しないでください。

---

## 14. スタンドアロン運用（1台完結）

Google Driveを使わず、1台のMacだけでスクレイピング・閲覧を完結させる方法です。

### 特徴

- `credentials.json` / `token.json` / Google Driveアプリ すべて不要
- セッションデータ・画像は Mac ローカルの `aucfan_tool/リサーチ結果/` に保存される
- 外出先など Google Drive に接続できない環境でも動作する

### 設定手順

`~/Downloads/aucfan_tool/.env` を以下の内容で作成（または編集）します。

```dotenv
GEMINI_API_KEY=AIzaSy...（取得済みのキー）
SITE_ROLE=scraper
GDRIVE_UPLOAD_ENABLED=false       # GDrive アップロード無効
OUTPUT_BASE_DIR=リサーチ結果       # ローカルフォルダに保存
SELLERS_MASTER_PATH=data/sellers_master.json
```

`.env` を保存後、`bash ~/Downloads/aucfan_tool/start.sh` で起動すれば完了です。

### スタンドアロンから2台運用に移行する場合

1. Google Drive for Desktop をインストールして `shinozakistore@gmail.com` でサインイン
2. `credentials.json` を配置して `setup_gdrive_auth.py` で認証
3. `.env` から `OUTPUT_BASE_DIR=リサーチ結果` と `SELLERS_MASTER_PATH=data/...` を削除（または `#` でコメントアウト）
4. `GDRIVE_UPLOAD_ENABLED=true` に変更
5. アプリを再起動

既存のローカルセッションデータ（`aucfan_tool/リサーチ結果/` 内）は手動で Google Drive の `AucFanToolData/リサーチ結果/` にコピーすると引き継げます。

---

## 15. Macの役割を入れ替える方法

`switch_role.sh` スクリプトを使うと、ワンコマンドで役割を切り替えられます。`.env` の手作業編集は不要です。

### 切り替えコマンド

**ターミナルで以下を実行するだけです。**

```bash
# このMacでスクレイピングしたい場合
bash ~/Downloads/aucfan_tool/switch_role.sh scraper

# このMacで閲覧専用にしたい場合
bash ~/Downloads/aucfan_tool/switch_role.sh reader

# このMac1台だけで完結させたい場合（Google Drive不要）
bash ~/Downloads/aucfan_tool/switch_role.sh standalone
```

コマンドを実行すると `.env` が自動で更新されます。残りの手動作業（Google Driveアプリのモード変更など）は画面に表示されます。

### scraper → reader への切り替え後の手動作業

1. **Google Driveアプリを「ミラーリング」に変更する**
   - Google Driveアプリを開く → 環境設定 → マイドライブの同期オプション
   - 「このデバイスにファイルをミラーリング」を選択 → 保存
2. GDriveの同期完了を待つ
3. `bash start.sh` でアプリを再起動

### reader → scraper への切り替え後の手動作業

1. **Google Driveアプリを「ストリーミング」に変更する**
   - Google Driveアプリを開く → 環境設定 → マイドライブの同期オプション
   - 「ファイルをストリーミング」を選択 → 保存
2. `credentials.json` を `~/Downloads/aucfan_tool/` に配置（初回のみ）
3. 初回GDrive認証（初回のみ）：
   ```bash
   cd ~/Downloads/aucfan_tool
   .venv/bin/python setup_gdrive_auth.py
   ```
4. `bash start.sh` でアプリを再起動

### 注意事項

- `.env` を変更したらアプリの**再起動が必須**です（`Ctrl+C` で停止 → `bash start.sh` で再起動）
- 2台のMacで同時に scraper にすると Google Drive への書き込み競合が起きることがあります。どちらか一方だけ scraper にしてください
- どちらのMacのユーザー名が違っていても `config.py` が自動でホームディレクトリを検出するため問題ありません

---

## 16. Excelリサーチシートの出力

スクレイピング結果の商品カードに **📗 Excel** ボタンが表示されています。このボタンを押すと、その商品1件分のリサーチシートが Excel ファイルとして自動保存されます。

### 保存先（フォルダ構成）

📗 Excel ボタンを押すと、以下のフォルダ構成が**自動で作成**されます。

```
Google Drive / AucFanToolData / リサーチ結果 /
  商品タイトル/                    ← 商品名のフォルダ（自動作成）
    商品タイトル_リサーチ.xlsx     ← Excelファイル本体
    amazon/                        ← Amazonライバル画像フォルダ（自動作成）
    1688/                          ← 1688仕入れ画像フォルダ（自動作成）
```

Amazon ライバルの商品画像は追記時に `amazon/ASIN番号/01.jpg, 02.jpg...` として保存されます。

### 操作手順

1. STEP 1〜3 のスクレイピング結果がグリッドに表示された状態にする
2. 保存したい商品カードの **📗 Excel** ボタンをクリック
3. ボタンが ⏳ に変わり、数秒後に「📗 Excel保存: 商品タイトル_リサーチ.xlsx」というポップアップが表示されれば完了
4. Google Drive の `AucFanToolData/リサーチ結果/` にファイルが作成されます

### scraper Mac・reader Mac どちらでも出力できます

Excel出力は scraper（スクレイピング側）でも reader（閲覧側）でも使えます。

| Mac | 画像の読み込み元 |
|---|---|
| scraper Mac | ローカルの `img_cache/セッション名/images/` |
| reader Mac | GDriveミラーリング済みの `AucFanToolData/リサーチ結果/セッション名/images/` |

reader Mac で Excel 出力するには、事前に scraper Mac でのスクレイピングが完了し GDriveへのアップロードが終わっている必要があります。また reader Mac の GDrive が「ミラーリング」モードで同期済みである必要があります（「ストリーミング」では画像が手元にないため画像なしでの出力になります）。

### Excel の内容

| 列 | 内容 |
|---|---|
| No | 1（固定） |
| 画像 | 商品サムネール画像 |
| 日付 | エクスポートした日付 |
| 代表キーワード | 商品タイトル |
| 件数（直近30日） | 直近30日の落札件数 |
| 4件以上 | 件数が4件以上なら ✓、未満なら ✗ |
| 最安値（価格＋送料） | グループ内の最安値 |
| セラーID①②③ | 出品しているセラーのID（最大3件） |
| 検索URL（Aucfan） | Aucfan の検索結果URL |
| 備考 | 手入力用（空白） |

### 書式を変更したいとき

`~/Downloads/aucfan_tool/リサーチ_テンプレート.xlsx` を Excel で開いて自由に書式を編集してください。保存後は次回のエクスポートからすぐに反映されます。コードの変更は不要です。

> テンプレートファイルを誤って削除・破損させた場合は、ターミナルで以下を実行して再生成できます：
> ```bash
> cd ~/Downloads/aucfan_tool
> python3 build_template.py
> ```

---

## 17. リサーチ追記ツール（/research）

**📗 Excel ボタンでExcelを作成したあと**、このツールでAmazonライバルのデータを次々と追記していきます。

### アクセス方法

- ブラウザで `http://localhost:5001/research` を直接開く
- または Amazon確認タブの **「📊 リサーチ追記ツールを開く」** ボタンを押す（別ウィンドウで開く）

### 操作手順

```
① Excelファイルを選択
   → 画面にファイル一覧が表示される（Google Driveの リサーチ結果/ フォルダを自動スキャン）
   → ファイルをクリックして選択（現在のAmazon取得件数が表示される）

② Amazon URL を貼り付けて「🔍 取得 → 追記」
   → amzn.asia/d/... の短縮URL でも対応
   → Seleniumが新規タブで商品ページを開き、商品情報を取得してExcelに追記
   → 商品画像は全枚数を amazon/ASIN/ フォルダに保存

③ 必要なだけ ② を繰り返す（ライバルが複数いる場合）

④ 取得完了後に「💴 FBA料金シミュレータで開く」ボタン（紫色）
   → SellerCentralのFBA料金シミュレータにASINを自動入力
   → ※要ログイン（2段階認証は事前に手動でログイン）

⑤ 「⬇ Excelをダウンロード」ボタンでブラウザにダウンロード可能
```

### Excelに追記される内容

**②Amazonライバル シート（1ライバル1行）**

| 列 | 内容 |
|---|---|
| ASIN | 商品ID |
| タイトル | 商品タイトル |
| 価格 | 現在価格 |
| 評価 | 星評価（例: 4.1） |
| レビュー数 | 総レビュー数 |
| A+ | あり / なし |
| URL（実） | リダイレクト後の正式URL |
| 入力URL | 貼り付けた短縮URLそのまま |
| 画像 | メイン画像（Excelに埋め込み） |

**③Amazonテキスト シート（縦並び・スペック文参考用）**

| 行ラベル | 内容 |
|---|---|
| タイトル | 商品タイトル |
| 価格 | 価格 |
| 商品の特徴 | 箇条書き（ページ上部の特徴欄） |
| 商品説明 | 商品説明テキスト |
| 仕様・詳細 | 商品情報テーブル（機能と仕様・サイズ等） |

> **「商品情報」テーブル（機能と仕様・取り付けタイプ・サイズ等）は「仕様・詳細」行に入ります。**  
> ページ上の折りたたまれた仕様テーブルも自動展開してから取得します。

### FBA料金シミュレータ連携

URL: `https://sellercentral.amazon.co.jp/revcal?ref=RC2nonlogin`

- SellerCentralへのログインが必要（2段階認証あり）
- **手動でログイン後は**、「💴 FBA料金シミュレータで開く」ボタンでASINが自動入力される
- 既にシミュレータタブが開いていれば再利用される（新しいタブを無駄に増やさない）

---

## 18. Amazon調査タブ（メインアプリ）

商品カードから **🔍 Amazon調査** ボタンを押すと、その商品に紐づいたAmazon調査ができます。

### 基本ワークフロー

```
① 商品カードの「🔍 Amazon調査」ボタンを押す
② Chrome でライバルの Amazon 商品ページを開く
③ 「🛒 今開いているAmazonページから取得」ボタンを押す
④ 別のライバルを調べる場合は ② に戻って繰り返す
⑤ 「📗 Excel」ボタンで Excel を作成
⑥ 「📊 リサーチ追記ツールを開く」で /research ページを開いて追記作業へ
```

---

## 19. 今後追加予定の機能

### 1688調査（/research ③欄・実装中）

/research ページの③1688欄は現在グレーアウトしています。実装後の操作イメージ：

- Chrome で **Aliprice の画像検索** → 1688商品ページへ移動
- URL を貼り付けて「取得 → 追記」でショップ情報・バリアント（色・サイズ等）・単価・MOQを取得
- 商品画像が `1688/商品ID/` フォルダに保存される
- Sheet4（④1688仕入れ）に追記 → 原価・利益・利益率が自動計算

### 利益計算（Sheet4）

| 項目 | 内容 |
|---|---|
| 原価（円） | 単価（元）× 35（送料・関税・代行手数料込み係数） |
| 利益 | 販売価格 − 原価 − FBA手数料 |
| 利益率 | 利益 ÷ 販売価格 |
| 判定 | 利益率 ≥ 25% かつ 利益 ≥ 450円 → ◎ |

### Claude Excel アドイン連携（検討中）

Excel から直接 Claude に指示できるアドインとの連携。詳細は未定。

---

*最終更新: 2026年5月26日（/research ページ・FBAシミュレータ連携・フォルダ構成・全画像保存を追記）*
